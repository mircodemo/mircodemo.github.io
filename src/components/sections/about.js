import React, { useEffect, useRef } from 'react';
import { StaticImage } from 'gatsby-plugin-image';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(140px, 200px));
    grid-gap: 0 10px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--green);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;
const StyledPic = styled.div`
  position: relative;
  max-width: 300px;

  @media (max-width: 768px) {
    margin: 50px auto 0;
    width: 70%;
  }

  .wrapper {
    ${({ theme }) => theme.mixins.boxShadow};
    display: block;
    position: relative;
    width: 100%;
    border-radius: var(--border-radius);
    background-color: var(--green);

    &:hover,
    &:focus {
      outline: 0;
      transform: translate(-4px, -4px);

      &:after {
        transform: translate(8px, 8px);
      }

      .img {
        filter: none;
        mix-blend-mode: normal;
      }
    }

    .img {
      position: relative;
      border-radius: var(--border-radius);
      mix-blend-mode: multiply;
      filter: grayscale(100%) contrast(1);
      transition: var(--transition);
    }

    &:before,
    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: var(--border-radius);
      transition: var(--transition);
    }

    &:before {
      top: 0;
      left: 0;
      background-color: var(--navy);
      mix-blend-mode: screen;
    }

    &:after {
      border: 2px solid var(--green);
      top: 14px;
      left: 14px;
      z-index: -1;
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = ['Microsoft Entra ID & Conditional Access', 'Secure Authentication Strategies', 'Microsoft Defender Suite', 'Qualys Vulnerability Management', 'CIS Controls Mapping & Analysis', 'Security Strategy & Advisory'];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="numbered-heading">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
              Hello! My name is Mirco Demo, and I’m passionate about safeguarding digital environments and empowering organizations to strengthen their cybersecurity foundations. 
              With over 25 years of experience in the IT sector, I’ve developed deep expertise in security analysis, auditing, and vulnerability management.
              My current focus is securing Microsoft ecosystems—including Microsoft 365, Microsoft Defender, and Microsoft Entra ID.
            </p><p> 
            In the realm of identity and access management, I work extensively with Microsoft Entra ID to enforce secure access policies. I design Conditional Access strategies tailored to real-world risk scenarios, ensuring that users connect safely based on context such as device compliance, user risk level, or geographic location. I also implement secure authentication methods like passwordless sign-in, multifactor authentication (MFA), and certificate-based access to minimize attack surface and boost user experience.              
            </p><p>
            I specialize in identifying threats, managing incidents, and fine-tuning security configurations to meet enterprise standards. 
              I also work hands-on with vulnerability management tools like Qualys and Microsoft Defender for Endpoint. 
              My approach goes beyond detection: I prioritize threats intelligently, guide remediation efforts, 
              and help teams make informed decisions that enhance their security posture. 
            </p><p>
              In addition to technical work, I’m experienced in conducting assessments and audits aligned with CIS Controls and ISO/IEC 27001 standards. 
              I help organizations recognize gaps, review controls, and implement practical improvements for long-term resilience.
            </p>
              <p>Here are a few technologies and practices I’ve been working with recently:</p>

          </div>

          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
        </StyledText>

        <StyledPic>
          <div className="wrapper">
            <StaticImage
              className="img"
              src="../../images/60607267.png"
              width={500}
              quality={95}
              formats={['AUTO', 'WEBP', 'AVIF']}
              alt="Headshot"
            />
          </div>
        </StyledPic>
      </div>
    </StyledAboutSection>
  );
};

export default About;
